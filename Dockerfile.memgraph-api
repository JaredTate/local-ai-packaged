FROM python:3.11-slim

WORKDIR /app

# Install dependencies
RUN pip install --no-cache-dir flask flask-cors neo4j gunicorn

# Copy the API script
COPY memgraph_api_proxy.py .

# Run with gunicorn for production
CMD ["gunicorn", "--bind", "0.0.0.0:5000", "--workers", "4", "memgraph_api_proxy:app"]